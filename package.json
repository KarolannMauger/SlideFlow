{
  "name": "slideflow",
  "private": true,
  "version": "1.0.0",
  "description": "Presentation HTML simple basee sur Reveal.js",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && http-server dist -p 5173",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "reveal.js": "^5.2.1"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "http-server": "^14.1.1"
  }
}
